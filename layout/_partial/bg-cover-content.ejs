<div class="row">
    <div class="col s10 offset-s1 m8 offset-m2 l8 offset-l1">
        <div class="brand">
            <div class="title">
                <% if (config.subtitle && config.subtitle.length > 0) { %>
                <%= config.subtitle %>
                <% } else { %>
                    subtitle
                <% } %>
            </div>
        </div>
    </div>
</div>
